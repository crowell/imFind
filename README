Android EC327 project
takes a picture, saves a bitmap to /sdcard/imFind.jpg
when you do recognize, the string that you input is saved to /sdcard/currentlearned.txt

recognize and learn not implemented.

GUI is finished, but can still be modified to do more things.
